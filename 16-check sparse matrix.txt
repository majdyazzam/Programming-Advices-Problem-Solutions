

#include <iostream>
#include<iomanip>
#include<string>
using namespace std;


void Print2DArray(int arr1[3][3], int row, int col) {

	for (int i = 0; i < row; i++) {
		for (int j = 0; j < col; j++) {
			//cout  << arr1[i][j] << setw(5);
			printf("%0*d   ",2,arr1[i][j]);
		}
		cout << "\n";
	}
}


int CountNumber(int arr[3][3],int row,int col,int num) {

	int count = 0;
	for (int i = 0; i < row; i++) {
		for (int j = 0; j < col; j++) {
			if (arr[i][j]==num) {
				count++;
			}
		}
	}
	return count;
}

bool CheckSparseMatrix(int arr[3][3], int row, int col) {
	/*
	int num = CountNumber(arr, row, col, 0);
	if (num >= (row * col) / 2)
		return true;
	else
		return false;
		*/
	short matrixSize = row * col;
	return (CountNumber(arr, row, col, 0) >= (matrixSize / 2));

}
int main()
{

	
	int arr1[3][3] = { {7,0,5},{4,7,0},{1,0,7} };

	Print2DArray(arr1, 3, 3);

	if (CheckSparseMatrix(arr1, 3, 3))

		cout << "Yes : It is sparse .\n";
	else
		cout<< "No : It is Not sparse .\n";
	return 0;
}

